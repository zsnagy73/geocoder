parameters:
  annotated_plugins_auto_discovery.geocoder:
    - { owner: 'geocoder', type: 'Provider', directory: 'Plugin/GeocoderProvider', class: 'Drupal\geocoder\Component\Annotation\GeocoderProviderPlugin'}

services:
  geocoder.http_adapter:
    class: \Ivory\HttpAdapter\CurlHttpAdapter